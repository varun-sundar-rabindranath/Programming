#include <iostream>
#include <vector>
#include <string> 

using namespace std;

class NextOrPrev {
public :
  int getMinimum (int nc, int pc, string start, string goal) {
    if(start.length() != goal.length()) { return -1; }

    int cost = 0;

    for(int i = 0; i < start.length(); i++) {
    }

  }

};
