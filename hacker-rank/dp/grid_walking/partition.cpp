#include <iostream>
#include <vector>
using namespace std;

void rec(int sum, vector<int> part){
  for(int i = 1; i < sum; i++) {
  }
} 

int main() {
  int sum = 3;
  vector<int> kick_off;
  kick_off.push_back(sum);
  rec(sum, kick_off);
  return 0 ;
}
